import { Actions } from '../../actions';
import { FormArrayState } from '../../state';
export declare function startAsyncValidationReducer<TValue>(state: FormArrayState<TValue>, action: Actions<TValue[]>): FormArrayState<TValue>;
