import { EventEmitter, OnInit } from '@angular/core';
import { Choice, Question } from '../../pages.model';
import { SurveyResponses } from '../../survey-response.model';
import { PagesService } from '../../../shared/pages-services/pages.service';
export declare class ChoiceComponent implements OnInit {
    private pageService;
    isActive: boolean;
    choiceQuestion: Question;
    surveyResponse: SurveyResponses;
    submitEvent: EventEmitter<{}>;
    isLastQuestion: boolean;
    private _isMultipleChoice;
    isMultipleChoice: boolean;
    constructor(pageService: PagesService);
    ngOnInit(): void;
    submit(): void;
    readonly selectedOptions: Choice[];
    private changeSelection;
    onClickOthers(): void;
    onInputOthers(): void;
    onOptionSelectionChange($event: any): void;
    onSingleSelect(identifier: string): void;
    isDisabled(choice: Choice): boolean;
}
