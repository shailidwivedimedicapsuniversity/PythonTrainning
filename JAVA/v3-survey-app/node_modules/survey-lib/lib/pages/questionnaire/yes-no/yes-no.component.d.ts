import { OnDestroy, OnInit } from '@angular/core';
import { Question } from '../../pages.model';
import { SurveyResponses } from '../../survey-response.model';
import { PagesService } from '../../../shared/pages-services/pages.service';
export declare class YesNoComponent implements OnInit, OnDestroy {
    private pageService;
    isActive: boolean;
    yesNoQuestion: Question;
    surveyResponse: SurveyResponses;
    private submitEvent;
    constructor(pageService: PagesService);
    ngOnInit(): void;
    onSelect(value: string): void;
    ngOnDestroy(): void;
}
