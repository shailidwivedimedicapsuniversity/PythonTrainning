import { EventEmitter, OnDestroy, OnInit } from '@angular/core';
import { Page } from '../pages.model';
import { ExpressionBuilder } from './expression-builder';
import { MandatoryQuestionValidation } from './mandatory-question-validation';
import { PagesService } from '../../shared/pages-services/pages.service';
import { SurveyLibService } from '../../survey-lib.service';
export declare class QuestionnaireComponent implements OnInit, OnDestroy {
    pagesService: PagesService;
    surveyLibService: SurveyLibService;
    private previousPageEvent;
    currentQuestionIndex: number;
    submitResponse: EventEmitter<string>;
    questionnairePage: Page;
    mandatoryQuestion: MandatoryQuestionValidation;
    expressionBuilder: ExpressionBuilder;
    isMandatoryQuestionAnswered: boolean;
    private nextQuestionSubscription;
    private previousQuestionSubscription;
    constructor(pagesService: PagesService, surveyLibService: SurveyLibService);
    ngOnInit(): void;
    nextQuestion(skipAnimation?: boolean): void;
    previousQuestion(): void;
    private jump;
    private logicJump;
    private defaultJump;
    private static isBackwardJump;
    private backwardJump;
    private forwardJump;
    private removeResponses;
    private updateProgressBar;
    ngOnDestroy(): void;
    private showErrorMessage;
    private animateExitNext;
    private animatePreviousExit;
}
