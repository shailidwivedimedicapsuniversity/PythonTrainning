import { EventEmitter, OnDestroy, OnInit } from '@angular/core';
import { Question } from '../../pages.model';
import { SurveyResponses } from '../../survey-response.model';
import { PagesService } from '../../../shared/pages-services/pages.service';
import { SurveyLibService } from '../../../survey-lib.service';
export declare class LongTextComponent implements OnInit, OnDestroy {
    private pageService;
    surveyLibServices: SurveyLibService;
    longText: Question;
    surveyResponse: SurveyResponses;
    submitEvent: EventEmitter<{}>;
    isLastQuestion: boolean;
    constructor(pageService: PagesService, surveyLibServices: SurveyLibService);
    ngOnInit(): void;
    onInputValue(value: string): void;
    skipQuestion(): void;
    ngOnDestroy(): void;
}
