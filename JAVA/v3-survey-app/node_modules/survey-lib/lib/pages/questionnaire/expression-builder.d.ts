import { Condition } from '../pages.model';
import { SurveyResponses } from '../survey-response.model';
export declare class ExpressionBuilder {
    private readonly IS_EQUALS_TO;
    private readonly IS_NOT_EQUALS_TO;
    private readonly IS_LESS_THAN;
    private readonly IS_GREATER_THAN;
    private readonly IS_LESS_THAN_OR_EQUALS_TO;
    private readonly IS_GREATER_THAN_OR_EQUALS_TO;
    private readonly IS;
    private readonly IS_NOT;
    private readonly IS_ON;
    private readonly IS_NOT_ON;
    private readonly IS_BEFORE;
    private readonly IS_AFTER;
    private readonly IS_BEFORE_OR_ON;
    private readonly IS_AFTER_OR_ON;
    private readonly IS_BETWEEN;
    private readonly IS_BEGINS_WITH;
    private readonly IS_ENDS_WITH;
    private readonly CONTAINS;
    private readonly DOES_NOT_CONTAINS;
    build(conditions: Condition[], responses: SurveyResponses[], questionType: string): string;
    private evaluateChoiceConditions;
    private evaluateRatingConditions;
    private evaluateNPSConditions;
    private evaluateYesNoConditions;
    private evaluateDateConditions;
    private evaluateTextConditions;
}
