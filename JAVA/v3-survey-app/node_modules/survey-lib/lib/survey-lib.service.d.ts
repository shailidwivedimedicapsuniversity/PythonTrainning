import { EventEmitter } from '@angular/core';
import { SelectedThemeModel } from './pages/pages.model';
export declare class SurveyLibService {
    currentElement: CurrentElement;
    enableSubmit: boolean;
    defaultThankYouPage: boolean;
    isEmbededInDesigner: boolean;
    questionAnsweredEventEmitter: EventEmitter<any>;
    isQuestionCardCampaign: boolean;
    currentElementEventEmitter: EventEmitter<CurrentElement>;
    constructor();
    initializeSurveyTheme(selectedThemeModel: SelectedThemeModel): void;
}
export declare class CurrentElement {
    currentPageIndex: number;
    currentQuestionIndex: number;
}
